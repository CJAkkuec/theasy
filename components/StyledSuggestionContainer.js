import styled from "styled-components";

const StyledSuggestionContainer = styled.section`
  display: flex;
  flex-direction: column;
  gap: 10px;
  width: 100%;
  max-width: 600px;
  padding: 20px;
  border: 1px solid var(--primary);
  border-radius: 10px;
`;

export default StyledSuggestionContainer;
